---
agent-notes: { ctx: "session handoff for the v3 decomposed-team branch; tracked so a fresh session on any model can resume", deps: [docs/adrs/meta/0015-decomposed-team.md, docs/methodology/team-layers.md, team/README.md, scripts/compose-team.mjs, scripts/team-log.mjs, docs/history/tracking/2026-09-09-adr-0015-decomposed-team-dispositions.md], state: active, last: "claude@2026-09-10", key: ["branch claude/summon-team-v3-decomposed-jyiur2, all pushed; wave 2 (the port) landed", "ADR-0015 Proposed, direction approved by the human; ratification waits on Archie's gate read", "next: the line as a dispatch workflow (step 6); owed: the Vik seam review, and the first real negative-control run"] }
---

# Session Handoff — Summon v3, wave 1

**Created:** 2026-09-09
**Branch:** `claude/summon-team-v3-decomposed-jyiur2` (both `summon` and `tech-blogger`; only `summon` carries code)
**Board item:** #138 (epic). No board status set; the session had no `gh`.
**Session summary:** Rebuilt the deprecated v2 roster as four composed layers plus a runtime record. The human approved the direction ("fold into 15 and go, I approve") and is switching models for implementation.

## Read these first, in this order

1. `docs/adrs/meta/0015-decomposed-team.md` — the decision, including § The runtime and the nine-step sequencing.
2. `docs/methodology/team-layers.md` — the canon spec every file under `team/` follows.
3. `team/README.md` — the tree and the commands.
4. `docs/history/tracking/2026-09-09-adr-0015-decomposed-team-dispositions.md` — what the human decided and why each reviewer finding was handled the way it was.

The published post that motivated all of this is in the sibling repo: `../tech-blogger/published/i-killed-my-agent-team-2026-08-15.md`.

## What was done (three commits)

- `c47112a` — ADR-0015, the layer spec, `team/` with five roles (reviewer with five lenses, coder, tester, architect, challenger), six personas (vik, tara, sato, pierrot, archie, wei), two skins, two adapters, the `summon-core` party, and `scripts/compose-team.mjs` with tests written first.
- `4b7219d` — Wei's Architecture Gate challenge (11 items, 2 blocking) and the Vik/Tara composer reviews, all dispositioned in the same wave: floor + conditional lenses in formations, the additive Dissent rule enforced by the composer, checks split deterministic from judged, adapter `keys`/`paths`/`formation`, the isolation test, reversal triggers, the Supersedes line for the register stack.
- `662942b` — the event log (`team/events.json`, `.summon/team-log.jsonl`, `scripts/team-log.mjs`), the `tdd` line with order and distinct-instance constraints checked over the log, `disagreement-rate` and `line-respected` bound as deterministic checks, the table renderer, role-level skin entries, the Log section in every composed agent.

## Current state

- **Working tree:** clean. Everything is pushed.
- **Tests:** 216 in `scripts/` (`pnpm test:scripts`: dispatch 58, compose 38, log 33, check-canon 20, review-wave 11, runner 10, plus the rest), 162 in `packages/summon-team` (`pnpm test`). `pnpm check:canon` green.
- **Compose:** `pnpm team:compose` → `build/team/` (gitignored), 18 files. Real party on claude-code: 24 of 46 boundaries tool-enforced, 22 prose; 29 of 46 checks bound to a command, 17 judged, and every bound check now sits on a seat that may `run` (the composer refuses the other shape).
- **Log:** the two composer reviews, Sato's check events from the runner, the prose negative-control run, and the two workflow runs (negative-control by the review station, `first-run` through the whole line). `pnpm team:control` passes on presence; `pnpm team:dissent` excludes the fixture and reports 2 real items judged, 1 non-unanimous, rate 0.50, short of its window of 10, exit 0.
- **Negative control:** passed on its first real run. The formation's return, verbatim from the log: "negative-control reviewed: 14 findings across 4 lenses; simplicity/test-quality/conformance revise, security veto; conditional lenses not applied". `pnpm team:control` reports ok: four lenses found something and the verdicts split. Reversal trigger 1 is not tripped.
- **`.claude/agents/`:** the 16 v2 files are untouched. Cutover is sequencing step 7 and waits on ratification; the human chose this explicitly.

## Decisions the human made this session (do not re-litigate)

1. Deterministic checks live on the role (`checks` in `role.json`), bound per project in `team/checks.json`.
2. The composer wires checks; a runner that executes them and binds receipts comes later (sequencing step 5).
3. `.claude/agents/` cuts over after ratification, not before.
4. v3 supersedes the register stack (#95, #105, #116, #120, #131). The human closes or rebases those PRs.
5. The runtime model (event log, line, work order, skin/renderer split) is folded into ADR-0015, not a separate ADR.

## Pending human input

- **Ratification.** ADR-0015 is Proposed with direction approved. Archie's independent gate read has not run. The human said "I approve" of the plan; treat ratification as a separate, explicit act. Do not flip Status to Accepted or start cutover without it.
- **The register-stack PRs** are still open on GitHub.
- **Running the review-wave workflow.** `team/workflows/review-wave.workflow.mjs` exists and is tested for shape only. Its first run against `team/fixtures/negative-control/` needs the human to opt in to the Workflow tool in a session; say so and it runs, then `review-wave.mjs ingest` writes the result to the log.
- **A correction to the published post** (v2's tester never had `disallowedTools`) is recorded in the ADR's Context; whether the post's sidecar gets a correction line is the human's call.

## Wave 2 (sequencing step 3): the port, landed the same day

All nine remaining v2 agents are ported: elicitor/cam, tracker/grace, product/pat, operator/ines (also holds reviewer/operational), designer/dani (also holds reviewer/accessibility, a new lens), data-scientist/debra, writer/diego, teacher/prof, cloud/cloud. A `write:infra` capability was added for the operator and cloud seats. The party is fifteen seats plus the review formation, whose conditional lenses now carry their personas. `code-reviewer.md` is retired: the formation carries its four lenses, its situational Ines lens, and its severity-ordered output; its review-document and sentinel apparatus is replaced by the coordinator writing records from returned messages (spec § Who writes what). The v2 files stay on disk until cutover.

Deletions per port, approximate by line: harness frontmatter, agent-notes directives, hybrid-phase tables, "when you're invoked" routing (now the party's job), the gh command block (lives in the tracking adapter), vendor price lists (perishable; the role says never guess at pricing). By non-blank line the nine v2 files were 776 lines; the v3 roles plus personas are 401 (48% fewer). See the wave-2 commit message for the per-file table.

**Review owed.** A Vik review of the ported tree (seam leakage between role and persona, fidelity of authorities such as Grace's debt escalation and Pat's proxy limits, the laziness ladder on the roles, and whether any declared check is a judgment in disguise) was started on 2026-09-09 and terminated by a session rate limit before writing a finding. Its skeleton file was deleted rather than kept, because a placeholder above a zero-findings sentinel is the anti-pattern `check-canon` #9 exists to catch. Re-run it as the first act of the next session, before ratification; the prompt is recoverable from this session's transcript, and the questions are listed in this paragraph.

## Wave 3 (sequencing step 4): check-canon learns the tree, landed

`scripts/check-canon.mjs` check #10 (`findTeamProblems`, exported and tested): every party composes through the real composer; every adapter is `fitted: true`; no skin string (title, class, epithet, blurb, sprite, alt) appears in any composed agent; every agent-notes dep under `team/`, the two team scripts, and the layer spec resolves; the configured log validates line by line. On its first real run it caught two skin epithets that repeated prompt prose (Sato's, Diego's); the epithets changed, the check did not.

## Wave 4 (sequencing step 5): the check runner and the negative control, landed

`scripts/run-checks.mjs` (9 tests): runs a seat's bound checks through the shell, binds each receipt to `{ head, dirty }`, writes `check` events, exits non-zero on a failure. `team-log.mjs control` (6 tests) and `team/fixtures/negative-control/` (a diff with one planted defect per floor lens) make reversal trigger 1 a bound check. `pnpm team:checks --seat <seat>`, `pnpm team:control`. The formation has not yet reviewed the fixture; the first real control run is owed, and it needs a model in the loop, not a script.

## Wave 5 (owed items and the first half of step 6), landed

- **Vik's seam review of the port** ran to completion (28 findings, 1 critical, sentinel present) and is dispositioned in `2026-09-09-persona-port-dispositions.md`: 23 applied in the tree, S4 and S8 deferred with reasons, S11 three of five left unbound honestly, F5's composer half deferred to the next composer wave. The critical (a bound check on a seat that must not run) is fixed twice: the check moved, and the composer refuses the shape. That refusal caught two more on its first run over the real tree; both are recorded in the dispositions.
- **The review station of the line as a workflow.** `scripts/review-wave.mjs` (11 tests, written first) does the two halves that need a filesystem: `prepare` reads the party formation and the diff, decides the conditional lenses from changed paths against the party's `paths` globs (F12: decided by a script, reported with the matching paths, never judged), and writes one schema-free prompt per lens carrying the role charter, the lens, and the persona and nothing from the skin or the adapter. `ingest` validates the workflow's return and writes `claim`, the non-refuted `finding`s, one `verdict` per lens, and `return` to the log. `team/workflows/review-wave.workflow.mjs` is the script the Workflow tool runs between them: one agent per lens with a schema-constrained verdict, then a skeptic per finding prompted to refute it. It has been parsed and its meta checked by test; **it has not been run.** Running it needs the human's explicit opt-in to the Workflow tool in a session, which is the earn gate ADR-0012 C describes. Until then the prose formation stays the floor, and the negative control was reviewed by the prose formation, not by this script.

## Wave 6 (the second half of step 6): dispatch and work orders, landed 2026-09-10

- **Work order and dispatcher.** The work order is a JSON file naming party, line, items, and instance counts (`team/events.json` `workOrder`; spec in `team-layers.md` § The work order). `scripts/dispatch.mjs` (58 tests, Tara first, Sato green, then a three-lens review and a second red/green round): `plan` validates the order, the seat names, the counts, and the adapter's `dispatch` limits, assigns one instance per station per item by one round-robin counter per seat, and refuses when a `distinct-instance` constraint would be broken rather than re-dealing; `open` validates everything, then creates worktrees, then writes `spawn` events, idempotent on a head and refusing a stale or foreign worktree; `claim` refuses an out-of-order or same-instance claim before writing. Adapters carry `dispatch: { concurrency, depth, isolation }` (`worktree | none`; skills: null). `treeState` now ignores `.summon/` so the log's own writes do not dirty the receipts. `pnpm team:dispatch`.
- **The line as a workflow.** `team/workflows/line.workflow.mjs` runs a plan: one pipeline per item, one agent per station as the composed seat's agent type, each station claiming through `dispatch.mjs claim` first. Tested for shape with the review-wave workflow; **not run** (same earn gate).
- **Review.** Vik 13 (0 critical), Tara 11 (2 critical, both on the tests), Pierrot 9 (0 critical); dispositions in `2026-09-10-dispatch-dispositions.md`. Decisions worth knowing: the log stays git-tracked on purpose (the checks read it in CI) and the tree binding excludes it; `claim` trusts self-written `return` events and canon now says so; `return` requires `ok` and `item`, and only `ok: true` from the claiming instance releases a station.
- **Owed from the review:** a shared test fixture module across the four script test files; receipt scrubbing and a root-bounded log path; `return` written from outside the model, which needs the hook enforcement level.

## Wave 7: the first real runs, on the human's opt-in (2026-09-10)

Both workflows ran; the full account is `2026-09-10-first-runs.md`. In short: the review station reviewed the negative control with five lenses (the operational lens triggered by the script on `src/**`), 45 agents, 34 findings survived 40 refutations, every lens vetoed, and the control's "verdicts must split" half failed on that unanimity even though every lens found its planted defect. The line ran one real item through red, green, and review with every claim written by the dispatcher and accepted or refused by it; the item sits at `revise` because the flag it added fails on the repo's own `package.json`, which has no version. Six findings against step 6 are listed in the report; the control's instrument was decided the same day (presence on the fixture, spread on real items); the root `package.json` version still needs the human. Owed from the runs: the log's place relative to worktrees, the line's review station taking the review-wave path before cutover, `ingest` not duplicating the dispatcher's claim, a stage-function test for the line workflow, and the skeptic stage's cost at scale.

## Next wave: the human's calls, then step 7

The control's instrument is decided and applied (presence on the fixture, spread on a full window of real items with the fixture excluded; `pnpm team:control` passes, `pnpm team:dissent` reports a short sample). The `package.json` question was decided (option 2: the flag reports the summon-team package's version) and `first-run` went back through the line: red, green, review, every claim through the dispatcher, three accepts and two revises, the dissent rate on real items now 0.50 over a two-item sample. One important finding remains and is the human's: the scaffolder ships the script into host projects that have no `packages/summon-team/`, so the flag would fail there; the item stays at `revise` on that alone. Still the human's after that: ratify ADR-0015, and cut over (step 7).

Both workflows exist and neither has run. The first run of `review-wave.workflow.mjs` against the negative-control fixture, then of `line.workflow.mjs` against a one-item order, needs the human's opt-in to the Workflow tool in a session; that is the acceptance test for step 6 and the evidence the ADR's earn gate asks for. After that, step 7 (cutover of `.claude/agents/` to composer output) waits on ratification.

## The step-5 notes as written before it landed

A runner that executes each bound check, records `{ id, grade, exit, receipt }` as a `check` event bound to `git rev-parse HEAD` plus a dirty flag (ADR-0012's receipt schema is owed to Archie first, or designed here as its first instance), and a planted-defect fixture the review formation must not wave through, which is reversal trigger 1's instrument. Then step 6, the line as a dispatch workflow.

## The original next-wave notes (now done)

Cam, Grace, Pat, Ines, Dani, Debra, Diego, Prof, Cloud, and the composite `code-reviewer` (already replaced by the `review-party` formation; confirm nothing in it is lost, then it is a delete). For each v2 file under `.claude/agents/`:

1. Classify every sentence as role, persona, skin, adapter, check, or delete. Expect delete to be the largest column; record the counts in the port's commit message so reversal trigger 5 has data.
2. Roles are Agent Skills folders with five required sections and a `role.json`; capabilities are work verbs, never tool names. Lenses go in `lenses/` if a role has several holders.
3. Personas need four sections and the Dissent must be additive; the composer refuses a bullet that restates the lens or role, so run `pnpm team:compose` after each port.
4. Add the persona to `team/parties/summon-core.json`, its skin entry to `team/views/jrpg-16bit/party.json` (classes and accents are in `docs/history/design/team-hero-sprites-16bit.md` § 3), and any checks a command can decide to `role.json` plus `team/checks.json`.
5. Ines's lens already exists as `reviewer/lenses/operational.md` and is a conditional in the review formation; her persona binds to an `operator` role.

Then step 4 (`check-canon` learns the tree) and step 5 (the check runner, receipts, `check` events, the negative-control fixture).

## Working rules that bit this session

- Composed files must not be hand-edited; edit the layer and recompose. `build/` is gitignored.
- The additive Dissent rule and the required-sections rule are enforced at compose time and will refuse a sloppy port; that is intended.
- Reviewers and challengers cannot write files. Gate records and review documents are written by the coordinator from the returned message, and a reviewer's sentinel must be the last line of its file; put coordinator dispositions in a companion file.
- Session entry protocol applies: `git status` clean, board item exists (#138), ADR before architecture, tests before implementation. Spawn Wei for anything architectural and Vik + Tara for code; their reviews this session each found real defects the coordinator had missed.
