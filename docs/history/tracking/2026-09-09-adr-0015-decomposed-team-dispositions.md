---
agent-notes: { ctx: "coordinator dispositions for Wei's ADR-0015 challenge and the Vik/Tara composer reviews", deps: [docs/history/tracking/2026-09-09-adr-0015-decomposed-team-debate.md, docs/history/code-reviews/2026-09-09-compose-team-vik.md, docs/history/code-reviews/2026-09-09-compose-team-tara.md, docs/adrs/meta/0015-decomposed-team.md], state: active, last: "claude@2026-09-09", key: ["11 Wei challenges: all accepted in full or part, none rejected outright except inline personas (C3)", "Vik 5 Important and Tara 2 Critical + 9 Important all fixed in the same wave", "the human answered four scoping questions the same day; recorded here"] }
---

# ADR-0015 — Dispositions (2026-09-09)

Answers to Wei's round-1 challenge (`2026-09-09-adr-0015-decomposed-team-debate.md`) and to the two composer reviews (`../code-reviews/2026-09-09-compose-team-vik.md`, `../code-reviews/2026-09-09-compose-team-tara.md`). Written by the coordinator; reviewers and challengers do not write files.

## The human's answers, same day

Four scoping questions were put to the human before these dispositions were made. Their answers, verbatim in substance: deterministic checks live on the role (`checks` in `role.json`, bound per project in `team/checks.json`); the composer wires checks now and a runner comes later; `.claude/agents/` cuts over after ratification, not on this branch; v3 supersedes the register stack.

## Wei's challenge

Each challenge is answered by a change in the tree, or by a recorded reason it is not. Wei's round-2 read has not run; the human decides at ratification.

| # | Disposition | Where |
|---|---|---|
| C1 | **Accepted, mechanism.** Formations now carry a `floor` (always) and `conditional` lenses, each with a `when` trigger the composer emits into the formation body and the 0012 review-wave workflow will evaluate. An `operational` lens (Ines's, from `docs/process/review-lenses.md`) is added as the first conditional. The ADR cross-references 0012 C and names the party formation as that floor. | `team/parties/summon-core.json`, `team/roles/reviewer/lenses/operational.md`, `scripts/compose-team.mjs`, ADR § Composition |
| C2 | **Accepted, mechanism, in two parts.** (a) The additive rule ships now: the composer refuses a Dissent bullet whose content words are mostly contained in a sentence of the bound lens or role, and the four paraphrasing sections Wei cited were rewritten to hold only what the lens does not say. (b) The disagreement-rate instrument and the negative-control fixture are the first two deterministic `checks` declared on the reviewer role, and are sequenced as the next wave with the receipt schema; the ADR's reversal trigger 1 keys on them. The human directed "wire now, run later" for checks. | `scripts/compose-team.mjs` (`assertAdditive`), `team/personas/*.md`, `team/roles/reviewer/role.json` (`checks`), ADR § Reversal triggers |
| C3 | **Accepted in part.** The four cited duplicates are removed. The additive check covers Dissent against lens and role. Inline personas for single-holder roles are **rejected**: the `skills` target needs the role alone, which is the whole reason the persona is a separate file, and the cost is priced in the ADR's Negative consequences with reversal trigger 2 watching it. | `team/personas/sato.md`, `tara.md`, `pierrot.md`, `archie.md`, `team/roles/coder/SKILL.md`, `architect/SKILL.md`, ADR § Alternatives |
| C4 | **Accepted.** Context item 4 rewritten: the presentation lived in the site, and the view layer is the site's data file made explicit, not a prompt saving. Pierrot's veto added to the security lens so the blurb is true. `Tells` is in the composition order and always was in the script; the ADR's order list was stale and now matches. | ADR § Context, `team/roles/reviewer/lenses/security.md`, ADR § Composition |
| C5 | **Accepted.** Adapter gains `keys` (frontmatter key names), `formation` (emission strategy), and `paths` (globs per write capability, for a future hook). `fitted` is `true` on both adapters, with the honesty in `review`. The isolation test composes the fixture on two different adapters and asserts the bodies are byte-identical. Delegation policy: recorded in the ADR as living in the adapter's `delegation` field when a harness needs one; Claude Code's does not exist yet because the injected instruction is not something a file can counter, and the ADR says so. | `team/harness/*.json`, `scripts/compose-team.test.mjs` (adapter isolation) |
| C6 | **Accepted in part.** The report text names three levels and reserves `hook` for when a hook composed from `paths` is installed; the composer emits only `tool` and `prose` because it cannot probe. `doctor` owns the final column, sequenced with 0012 E. A sentence in Context corrects the post's line 68: the tester's boundary was prose in v2 as well. | `scripts/compose-team.mjs` (`renderEnforcement`), ADR § Context, § Enforcement report |
| C7 | **Accepted.** The human confirmed v3 supersedes the register stack. The ADR carries a Supersedes line with the PR list stated once (#95, #105, #116, #120, #131). The human, as the stack's author, is the one telling its author. | ADR § Status |
| C8 | **Accepted verbatim.** Wei's five triggers, 90-day checkpoint 2026-12-08. | ADR § Reversal triggers |
| C9 | **Accepted; now true.** The composer, its tests, `build/` in `.gitignore`, and the `team:compose` script were committed in `c47112a` after Wei's read. The Status block is updated. The agent-notes dep-resolution rule for check-canon is deferred to sequencing step 3 with the other tree rules. | commit `c47112a`, ADR § Status |
| C10 | **Accepted.** The spec now states that gate records and review documents are written by the coordinator from the returned message. `team-governance.md`'s stale `docs/tracking/` path is corrected to `docs/history/tracking/`. | `docs/methodology/team-layers.md` § Who writes what, `docs/process/team-governance.md` |
| C11 | **Accepted.** The ADR decides: the scaffolder ships `team/` and the composer, runs compose at install, and `doctor` runs `compose --check`; sequenced after cutover. 0012 F is marked amended by this ADR in both files. | ADR § Sequencing, § Consequences; `docs/adrs/meta/0012-executable-canon.md` (amendment note) |

Blocking count after dispositions: 0 by the coordinator's reading; Wei's withdrawal is his to give, and the human's ratification is separate.

## Vik's review of the composer (18 findings, 0 Critical, 5 Important)

All five Important findings are fixed: missing or misspelled sections are refused with the file named (`requireSections`); JSON errors carry the path (`readJson`); a party that names a missing view is refused, and only a party with no view composes skinless; the spec's formation example is corrected to the object form and a string member is refused with the example in the message; the tests spawn `process.execPath` with explicit stdio and a timeout. Of the thirteen suggestions, twelve are applied (dead return, unused parameter, `persona.role`, the holds ternary, the two `lenses` names, the hoisted path and by-name dedupe, `--out` without a value, tmpdir cleanup, structural smoke assertions, the header moved into a frontmatter comment with a `summon:` marker for the cutover ceiling, Boundaries added to the spec and the reviewer role, the formation role guard). The thirteenth, CRLF tolerance, is left consistent with `check-canon` as Vik suggested. The `role.json` sidecar stands on Vik's reading of the ladder.

## Tara's review of the tests (20 findings, 2 Critical, 9 Important)

Both Criticals are fixed: a persona bound to two seats now needs an `as` name and the composer refuses two seats that compose to the same path, with the test asserting path uniqueness; the undeclared-lens refusal is tested through a persona that holds the role on any lens, so the holds check cannot fire first. All nine Important gaps have tests: `every` versus `some` (a boundary sharing one needed tool), the unmapped boundary (prose, now also in the spec), all nine sections asserted by marker in order, comment stripping, the widened view leak list including title, skin, and epithet, tool names in `must-not`, a formation member that does not hold its seat, the description line, and the order test extended through Boundaries and Output with the order written into the spec. All nine suggestions are applied. Tara's mutation harness was not re-run in this wave; the new cases were written against her named mutants, and the first run of the rewritten suite caught two arithmetic errors in the coordinator's own expected counts, which is the lens working as intended.
