node_modules/
dist/

# Composed team output (pnpm team:compose) — generated from team/, never hand-edited
build/
*.tsbuildinfo

.env
.env.*

.DS_Store
*.swp
*.swo
*~

# WSL / NTFS mark-of-the-web artifacts (alternate data streams)
*:Zone.Identifier

pnpm-debug.log*

# Astro build cache
.astro/

# Playwright MCP browser session scratch artifacts
.playwright-mcp/

# Per-session handoff scratch — regenerated by /handoff, never tracked (ADR-0007 §7)
.claude/handoff.md

# Dispatch worktrees (scripts/dispatch.mjs open) — one per seat instance, never tracked
.summon/worktrees/
.summon/plan.json
