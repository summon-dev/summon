{
  "$comment": "Project-level bindings for role checks: which command decides each claim in THIS repo. A check with no binding is judged by the model and graded inferential.",
  "tests-green": {
    "run": "pnpm test",
    "receipt": "the pass/fail summary lines, pasted"
  },
  "tests-red": {
    "run": "pnpm test",
    "receipt": "the failing test names and their assertion messages, pasted"
  },
  "tests-untouched": {
    "run": "test -z \"$(git diff --name-only HEAD -- '*.test.*' '*_test.*' test tests)\"",
    "receipt": "exit 0 means no test file differs from HEAD; on a non-zero exit, run the diff to see which files"
  },
  "debt-markers": {
    "run": "pnpm harvest:debt",
    "receipt": "the marker list, pasted"
  },
  "canon-drift": {
    "run": "pnpm check:canon",
    "receipt": "the line 'canon check: OK' or the failure list"
  },
  "adr-numbering": {
    "run": "pnpm check:canon",
    "receipt": "no 'ADR numbering' line in the failure list"
  },
  "dependency-audit": {
    "run": "pnpm audit --audit-level=moderate",
    "receipt": "the summary line"
  },
  "log": ".summon/team-log.jsonl",
  "disagreement-rate": {
    "run": "node scripts/team-log.mjs dissent --log .summon/team-log.jsonl --last 10 --exclude negative-control",
    "receipt": "the rate line: N items, M non-unanimous; exit 1 only on a full window with rate 0"
  },
  "line-respected": {
    "run": "node scripts/team-log.mjs check --log .summon/team-log.jsonl --line tdd",
    "receipt": "the summary line; a violation names the item, stations, and instance"
  },
  "contrast-motion": {
    "run": "pnpm check:css",
    "receipt": "the checker's summary line"
  },
  "ci-green": {
    "run": "pnpm check:canon && pnpm test",
    "receipt": "'canon check: OK' and the test summary lines"
  },
  "negative-control": {
    "run": "node scripts/team-log.mjs control --log .summon/team-log.jsonl --item negative-control --lenses simplicity,test-quality,security,conformance",
    "receipt": "the control line and the verdict per lens (verdicts are information; presence is the claim)"
  },
  "installs-and-builds": {
    "run": "pnpm install --frozen-lockfile && pnpm build",
    "receipt": "the last line of each command"
  },
  "product-context-present": {
    "run": "test -f docs/product-context.md && grep -q 'Last updated' docs/product-context.md",
    "receipt": "exit 0; this repo has not run kickoff, so it fails here honestly"
  }
}
